执行脚本的方法
1> bash 脚本名称 bash x
以调试模式来运行脚本
2> ./ 脚本名称 >
需要对脚本有可执行的权限

数字的判断 	字符的判断 	文件的判断
‐gt 大于 	‐z 空 		‐d 文件是不是一个目录
‐ge 大于等于 	= 字符相等 	‐f 是不是一个普通文件
‐lt 小于 	!= 字符不相等 	‐e 文件是不是存在
‐le 小于等于 	‐n 非空	-s文件存在否？
‐ne 不等于 	‐a 逻辑与
‐eq 等于 	‐o 逻辑或
